# 课程评价网站 - 完整需求和实现方案

## 项目概述

开发一个南开大学课程评价网站，允许学生对课程进行评价、评分和互动讨论。

## 核心功能需求

### 1. 用户认证系统
- **邮箱限制**：只允许 `@mail.nankai.edu.cn` 结尾的邮箱注册
- **用户名显示**：用户注册时设置用户名，显示时不展示邮箱和学号
- **隐私保护**：邮箱信息仅用户本人可见，其他人无法查看
- **访问权限**：未注册用户只能查看，无法进行评价

### 2. 课程搜索和展示
- **课程搜索**：支持按课程名称搜索
- **独立展示**：每个课程实例（不同教师组合）作为独立条目展示
- **课程信息**：显示课程名称、授课教师、学期、选课序号、开课单位、学分等

### 3. 评价系统
- **评分机制**：10分满分制，对应5颗星显示，支持半颗星
- **多次评价**：同一用户可对同一学期的同一课程进行多次评价
- **评价管理**：用户不可编辑已发布的评价，但可以删除自己的评价

### 4. 评论互动系统
- **回复功能**：评价可以被其他人回复
- **点赞功能**：支持对评价进行点赞
- **排序方式**：
  - 默认按热度排序（综合点赞数、回复数、时间衰减因子）
  - 可选择按时间排序

### 5. 内容审核系统
- **举报机制**：用户可举报不当评价内容并说明原因
- **管理员审核**：管理员审核举报请求，审核通过后删除相关内容

### 6. 数据可视化
- **统计图表**：类似提供的图片样式，显示课程评价趋势
- **柱状图**：各学期评价数量
- **折线图**：各学期平均评分趋势
- **交互提示**：悬浮显示具体数据

### 7. 课程数据导入
- **数据来源**：从南开大学教务处网站下载的选课手册PDF
- **手动运行**：PDF解析模块需要手动触发，优先级较低
- **半年更新**：选课通常半年进行一次

## 技术架构

### 后端技术栈
- **框架**：Python + FastAPI
- **ORM**：SQLAlchemy + Alembic（数据库迁移）
- **数据库**：PostgreSQL
- **缓存**：Redis（缓存热度数据和统计信息）
- **异步任务**：Celery（处理邮件发送等）
- **认证**：JWT

### 前端技术栈
- **框架**：React + TypeScript
- **UI组件库**：Ant Design（支持星级评分组件）
- **图表库**：ECharts（数据可视化）
- **状态管理**：React Query
- **表单处理**：React Hook Form
- **路由**：React Router

### 部署方案
- **容器化**：Docker
- **反向代理**：Nginx
- **部署环境**：云服务器或校内服务器

## 数据库设计

### 用户表 (users)
```sql
- id: 自增主键
- username: 用户名（注册时设置，用于显示）
- email_hash: 邮箱哈希（不可逆，仅用于验证）
- password_hash: 密码哈希
- is_verified: 邮箱验证状态
- created_at: 注册时间
- is_active: 账户状态
```

### 课程实例表 (course_instances)
```sql
- id: 自增主键
- course_name: 课程名称
- semester: 学期
- course_selection_number: 选课序号
- department_code: 开课单位代码
- department_name: 开课单位名称
- credits: 学分
- course_type: 课程类型
- instructors_hash: 教师组合哈希值
- unique_identifier: 复合唯一标识
- created_at: 创建时间
- updated_at: 更新时间
```

### 课程教师关联表 (course_instructor_relations)
```sql
- id: 自增主键
- course_instance_id: 课程实例ID (外键)
- instructor_name: 教师姓名
- created_at: 创建时间
```

### 评价表 (reviews)
```sql
- id: 主键
- user_id: 用户ID
- course_instance_id: 课程实例ID
- rating: 评分 (1-10)
- content: 评价内容
- created_at: 评价时间
- is_deleted: 软删除标记
- deleted_at: 删除时间
- deleted_by: 删除者（用户自己或管理员）
```

### 回复表 (replies)
```sql
- id: 主键
- review_id: 评价ID
- user_id: 回复用户ID
- content: 回复内容
- created_at: 回复时间
- is_deleted: 软删除标记
```

### 点赞表 (likes)
```sql
- id: 主键
- user_id: 用户ID
- review_id: 评价ID
- created_at: 点赞时间
```

### 举报表 (reports)
```sql
- id: 主键
- reporter_id: 举报人ID
- review_id: 被举报评价ID
- reason: 举报原因
- description: 详细说明
- status: 处理状态（pending/approved/rejected）
- admin_id: 处理管理员ID
- admin_comment: 管理员备注
- created_at: 举报时间
- processed_at: 处理时间
```

## 核心算法和标准

### 课程唯一标识生成
```python
# 生成规则
instructors_sorted = sorted(instructors_list)  # 确保顺序一致性
instructors_string = ",".join(instructors_sorted)
instructors_hash = hashlib.md5(instructors_string.encode()).hexdigest()[:8]
unique_identifier = f"{semester}_{course_selection_number}_{course_name}_{instructors_hash}"

# 示例：2025-2026-1_1001_高等数学A_a1b2c3d4
```

### 热度排序算法
```python
# 热度计算公式（示例）
def calculate_hotness(likes_count, replies_count, created_at):
    time_decay = calculate_time_decay(created_at)
    hotness = (likes_count * 2 + replies_count * 1.5) * time_decay
    return hotness
```

### PDF数据标准格式
```json
{
  "semester": "2025-2026-1",
  "extraction_date": "2025-08-18",
  "course_instances": [
    {
      "course_selection_number": "1001",
      "course_name": "高等数学A",
      "instructors": ["张三", "李四"],
      "department_code": "MATH",
      "department_name": "数学科学学院",
      "credits": 4,
      "course_type": "通识必修课"
    }
  ]
}
```

## API接口设计

### 核心API端点
```
# 用户认证
POST /api/auth/register - 用户注册
POST /api/auth/login - 用户登录
POST /api/auth/verify-email - 邮箱验证

# 课程相关
GET /api/courses/search - 课程搜索（支持筛选）
GET /api/courses/{unique_identifier} - 课程详情
GET /api/courses/{unique_identifier}/statistics - 课程统计数据

# 评价相关
GET /api/courses/{unique_identifier}/reviews - 课程评价列表
POST /api/courses/{unique_identifier}/reviews - 发布评价
DELETE /api/reviews/{id} - 删除评价
POST /api/reviews/{id}/like - 点赞/取消点赞
POST /api/reviews/{id}/reply - 回复评价

# 举报相关
POST /api/reviews/{id}/report - 举报评价
GET /api/admin/reports - 管理员查看举报列表
PUT /api/admin/reports/{id} - 处理举报

# 数据导入
POST /api/admin/import-courses - 导入课程数据
```

## 前端界面设计

### 搜索结果展示格式
```
数学分析 - 张三, 李四 (2025-2026-1)
选课序号: 1001 | 数学科学学院 | 4学分
⭐⭐⭐⭐☆ 4.2分 (15条评价)
```

### 课程详情页面结构
- 课程基本信息卡片
- 数据可视化图表（评价趋势）
- 评价列表（支持排序和筛选）
- 写评价功能区
- 教师信息链接

## 安全性和性能优化

### 安全措施
- 邮箱格式严格验证
- 密码哈希存储
- JWT token认证
- SQL注入防护
- XSS攻击防护
- 内容审核机制

### 性能优化
- Redis缓存热门课程和统计数据
- 数据库索引优化
- 分页查询
- 图片和静态资源CDN
- API响应压缩

### 防刷机制
- 内容相似度检测
- 异常行为监控
- IP和设备指纹识别
- 验证码机制

## 开发阶段规划

### 第一阶段：基础功能（2-3周）
- 用户注册登录系统
- 课程信息展示
- 基础评价功能
- 评价删除功能

### 第二阶段：交互功能（2周）
- 评论回复系统
- 点赞功能
- 举报审核系统

### 第三阶段：数据可视化（1-2周）
- 统计数据计算
- 图表展示功能
- 高级筛选和排序

### 第四阶段：优化完善（1周）
- 性能优化
- 安全加固
- 用户体验改进

### 第五阶段：扩展功能（独立开发）
- PDF解析模块
- 数据导入功能
- 管理后台完善

## 关键特性总结

1. **课程实例独立性**：每个教师组合的课程都是独立的评价对象
2. **多次评价支持**：用户可以对同一课程进行多次评价
3. **隐私保护**：用户邮箱信息完全隐藏
4. **数据可视化**：提供丰富的统计图表展示
5. **内容审核**：完整的举报和审核机制
6. **扩展性**：支持PDF数据自动导入（后期实现）

这个方案完整地满足了所有提出的需求，提供了清晰的技术实现路径和数据结构设计。
