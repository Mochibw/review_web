好的，遵照您的要求，我已经根据我们讨论的8点建议，对原始方案进行了全面的修订和优化。

这是一个修改后的、可以直接作为项目启动蓝图的**完整实现方案**。

---

# 课程评价网站 - 完整实现方案 (修订版)

## 项目概述

开发一个南开大学课程评价网站，允许通过邮箱验证的学生对课程进行评价、评分和互动讨论，并提供数据可视化功能，帮助学生进行课程选择。

## 核心功能需求

### 1. 用户认证系统
- **邮箱限制**：只允许 `@mail.nankai.edu.cn` 结尾的邮箱注册和验证。
- **用户名显示**：用户注册时设置用户名，在网站各处均使用用户名进行展示，不暴露邮箱和学号。
- **隐私保护**：邮箱信息仅用于身份验证和通知，对其他用户完全隐藏。
- **访问权限**：未登录用户只能查看课程和评价；登录并验证邮箱后才能进行评价、点赞、回复等操作。

### 2. 课程搜索和展示
- **课程搜索**：支持按课程名称、教师姓名进行模糊搜索。
- **独立展示**：每个课程实例（同一门课的不同教师组合或不同学期）作为独立条目展示。
- **课程信息**：显示课程名称、授课教师、学期、选课序号、开课单位、学分等。

### 3. 评价系统
- **评分机制**：10分满分制，对应5颗星显示，支持半颗星（0.5分）。
- **单一可编辑评价**：同一用户对同一学期的同一课程**只能发布一条评价**。
- **评价管理**：用户可以**随时编辑**或**删除**自己发布的评价。

### 4. 评论互动系统
- **回复功能**：任何已验证用户都可以对他人的评价进行回复，形成讨论串。
- **点赞功能**：支持对评价进行点赞/取消点赞，每个用户对每条评价只能点赞一次。
- **排序方式**：
  - 默认按**热度排序**（综合点赞数、回复数、时间衰减因子）。
  - 可选择按**时间排序**（最新或最早）。

### 5. 内容审核系统
- **举报机制**：用户可举报不当评价或回复，并填写举报原因。
- **管理员审核**：管理员在后台审核举报请求，审核通过后可删除相关内容。

### 6. 数据可视化
- **统计图表**：以图表形式直观展示课程的评价数据。
- **柱状图**：展示课程在各学期的评价数量分布。
- **折线图**：展示课程在各学期的平均分趋势。
- **交互提示**：鼠标悬浮在图表上时，显示该数据点的具体数值。

### 7. 课程数据导入
- **数据来源**：从南开大学教务处网站下载的选课手册PDF。
- **手动运行**：提供一个需要手动触发的脚本或后台接口，用于解析PDF并导入课程数据。
- **更新周期**：通常每学期开学前或选课前手动执行一次。

## 技术架构

### 后端技术栈
- **框架**：Python + FastAPI
- **ORM**：SQLAlchemy + Alembic (数据库迁移)
- **数据库**：PostgreSQL
- **缓存**：Redis (用于缓存热点数据、统计信息和API速率限制)
- **异步任务**：**FastAPI BackgroundTasks** (用于处理邮件发送等轻量级异步任务)
- **认证**：JWT (JSON Web Tokens)

### 前端技术栈
- **框架**：React + TypeScript
- **UI组件库**：Ant Design (提供成熟的表单、评分等组件)
- **图表库**：ECharts (或同类库，用于数据可视化)
- **状态管理**：React Query (或 SWR，用于API数据获取和缓存)
- **路由**：React Router

### 部署方案
- **容器化**：Docker & Docker Compose
- **反向代理**：Nginx
- **部署环境**：云服务器或校内服务器

## 数据库设计

### 用户表 (users)
```sql
- id: 自增主键
- username: 用户名 (唯一, 用于显示)
- email: 邮箱 (唯一, 存储加密后的原始邮箱，用于邮件发送)
- password_hash: 密码哈希
- is_verified: 邮箱验证状态 (Boolean)
- created_at: 注册时间
- is_active: 账户状态 (Boolean)
```

### 教师表 (instructors)
```sql
- id: 自增主键
- name: 教师姓名 (建立唯一索引)
- department_name: 所属院系 (可选)
- created_at: 创建时间
```

### 课程实例表 (course_instances)
```sql
- id: 自增主键
- course_name: 课程名称
- semester: 学期 (如 "2025-2026-1")
- course_selection_number: 选课序号
- department_name: 开课单位名称
- credits: 学分
- unique_identifier: 复合唯一标识 (用于URL和内部查询)
- created_at: 创建时间
- updated_at: 更新时间
```

### 课程教师关联表 (course_instructor_relations)
```sql
- id: 自增主键
- course_instance_id: 课程实例ID (外键 -> course_instances.id)
- instructor_id: 教师ID (外键 -> instructors.id)
- -- 确保同一门课不会重复关联同一个老师
- UNIQUE (course_instance_id, instructor_id)
```

### 评价表 (reviews)
```sql
- id: 主键
- user_id: 用户ID
- course_instance_id: 课程实例ID
- rating: 评分 (整数, 1-10)
- content: 评价内容 (Text)
- created_at: 评价创建时间
- updated_at: 评价最后编辑时间
- is_deleted: 软删除标记
- -- 确保一个用户对一门课只能有一条评价
- UNIQUE (user_id, course_instance_id)
```

### 回复表 (replies)
```sql
- id: 主键
- review_id: 评价ID
- user_id: 回复用户ID
- content: 回复内容
- created_at: 回复时间
- is_deleted: 软删除标记
```

### 点赞表 (likes)
```sql
- user_id: 用户ID (主键, 外键)
- review_id: 评价ID (主键, 外键)
- created_at: 点赞时间
- PRIMARY KEY (user_id, review_id) -- 复合主键，天然保证唯一性
```

### 举报表 (reports)
```sql
- id: 主键
- reporter_id: 举报人ID
- review_id: 被举报评价ID (也可扩展为举报回复)
- reason: 举报原因
- status: 处理状态 (pending/approved/rejected)
- created_at: 举报时间
```

## 核心算法和标准

### 课程唯一标识生成
```python
import hashlib

def generate_unique_identifier(semester, course_name, instructors_list):
    # 保证教师顺序一致性
    instructors_sorted = sorted(instructors_list)
    identifier_str = f"{semester}_{course_name}_{','.join(instructors_sorted)}"
    # 使用哈希避免URL过长或出现特殊字符
    return hashlib.md5(identifier_str.encode()).hexdigest()
```

### 热度排序算法
```python
from datetime import datetime, timedelta

def calculate_hotness(likes_count, replies_count, created_at):
    """
    计算评价的热度值，类似Hacker News的算法。
    - G: 重力因子，时间衰减的速度，值越大衰减越快。
    """
    gravity = 1.8 
    hours_since_creation = (datetime.utcnow() - created_at).total_seconds() / 3600
    
    # 基础分 = 点赞数 + 回复数 (可调整权重)
    base_score = likes_count + replies_count * 2
    
    # 时间衰减
    hotness = base_score / ((hours_since_creation + 2) ** gravity)
    return hotness
```

## API接口设计

```
# 用户认证
POST /api/auth/register          - 用户注册
POST /api/auth/login             - 用户登录
POST /api/auth/send-verify-email - 请求发送验证邮件

# 课程相关
GET  /api/courses/search         - 课程搜索
GET  /api/courses/{id}           - 课程详情
GET  /api/courses/{id}/statistics - 课程统计数据

# 评价相关
GET  /api/courses/{id}/reviews   - 获取课程的评价列表
POST /api/courses/{id}/reviews   - 发布评价
PUT  /api/reviews/{review_id}    - 编辑自己的评价
DELETE /api/reviews/{review_id}  - 删除自己的评价

# 互动相关
POST /api/reviews/{review_id}/like - 点赞/取消点赞
POST /api/reviews/{review_id}/reply - 回复评价

# 举报相关
POST /api/reviews/{review_id}/report - 举报评价
```

## 安全性和性能优化

### 安全措施
- **身份验证**：强制要求 `@mail.nankai.edu.cn` 邮箱验证，作为核心防线。
- **密码安全**：密码使用强哈希算法（如 Argon2 或 bcrypt）存储。
- **数据加密**：对数据库中存储的用户邮箱等敏感信息进行加密 (Data-at-rest encryption)。
- **API安全**：使用 JWT 进行无状态认证，并对所有需要权限的接口进行保护。
- **防SQL注入/XSS**：使用 ORM 参数化查询；前端对用户输入内容进行转义。

### 性能优化
- **缓存**：使用 Redis 缓存热门课程的详情、评价列表和统计数据。
- **数据库索引**：为常用查询字段（如外键、`unique_identifier`）建立索引。
- **分页查询**：所有列表接口（如评价列表）均采用分页返回。
- **API速率限制**：对注册、登录、发布评价等关键接口进行速率限制，防止暴力破解和恶意请求。

## 开发阶段规划

### 第零阶段：项目初始化与基础设施搭建 (1周)
- 搭建前后端开发环境（Docker Compose）。
- 初始化项目骨架（FastAPI, Create React App）。
- 配置代码规范与格式化工具（Ruff, Prettier）。
- 建立Git仓库及分支管理策略。

### 第一阶段：核心功能实现 (2-3周)
- 用户注册、登录、邮箱验证系统。
- 课程数据导入与展示，实现课程搜索。
- 评价的发布、编辑、删除功能。

### 第二阶段：互动与社区功能 (2周)
- 评价的回复与展示。
- 点赞/取消点赞功能。
- 评价列表的热度和时间排序。

### 第三阶段：数据可视化与管理 (1-2周)
- 后端统计数据API开发。
- 前端使用ECharts实现数据可视化图表。
- 基础的举报功能和后台审核接口。

### 第四阶段：优化与部署 (1-2周)
- 性能优化（缓存、数据库索引）。
- 安全加固（API速率限制、输入验证）。
- 编写部署脚本，完成线上部署。
- 全面测试和Bug修复。

## 关键特性总结

1. **课程实例独立性**：每个教师组合的课程都是独立的评价对象。
2. **评价可随时编辑**：用户可以持续更新自己对课程的看法。
3. **南开身份认证**：通过邮箱验证确保社区成员的真实性。
4. **数据驱动决策**：提供丰富的统计图表，帮助学生选择课程。
5. **轻量级架构**：采用 FastAPI BackgroundTasks，简化了项目初期的技术栈和运维复杂度。
6. **设计可扩展**：通过规范化的数据库设计（如独立的教师表），为未来的功能（如教师主页）预留了空间。
